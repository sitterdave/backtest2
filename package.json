{
  "name": "correlation-gap-backtest",
  "version": "1.0.0",
  "description": "Backtester for Correlation Gap Strategy on prediction markets",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "backtest": "ts-node src/index.ts",
    "selftest": "ts-node src/selftest.ts",
    "ui": "ts-node src/server.ts"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
